# =========================================================
# secrets.toml — InternshipLatam (Streamlit)
# À placer dans : .streamlit/secrets.toml (jamais commité sur GitHub)
# Ajoute ".streamlit/secrets.toml" à ton .gitignore !
# =========================================================

# --- Base de données (Neon PostgreSQL) ---
# Format compatible avec st.connection("postgres", type="sql")
[database]
db_host = "ton-projet.neon.tech"
db_name = "internshiplatam"
db_user = "ton_user"
db_password = "ton_password"
db_sslmode = "require"
db_channelbinding = "require"

# --- LLM / Enrichissement ---
[groq]
api_key = "gsk_xxx"
